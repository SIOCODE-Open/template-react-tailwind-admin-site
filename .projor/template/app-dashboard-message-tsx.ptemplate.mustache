{
    "map": {},
    "filename": "src/components/AppDashboardMessage.component.tsx",
    "formatUsing": "typescript"
}
---
import { Icon } from '@iconify/react';
import classNames from "classnames";

export function AppDashboardMessage(
    opts: {
        name: string,
        value: string,
    }
) {
    return <div className="w-full h-full flex flex-col justify-center items-start gap-3 p-6 bg-white rounded-xl border border-slate-200 shadow-sm">
        <h1 className="text-xl font-bold text-slate-800">{opts.name}</h1>
        <p className="text-sm text-slate-600 leading-relaxed">{opts.value}</p>
    </div>
}